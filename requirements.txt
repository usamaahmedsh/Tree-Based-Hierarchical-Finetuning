# Core async HTTP
aiohttp>=3.9.0
aiolimiter>=1.1.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0.1

# NLP / Text processing
nltk>=3.8.1
textstat>=0.7.3

# MinHash deduplication
datasketch>=1.6.4

# Topic modeling (Layer 1 — BERTopic)
bertopic>=0.16.0
sentence-transformers>=2.7.0
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3.0

# Deep learning backend
torch>=2.1.0
transformers>=4.42.0

# Emotion classification (Layer 2 — GoEmotions)
# Uses transformers pipeline — no extra package needed

# Demographic inference (Layer 3 — M3-Inference)
m3inference>=1.1.5

# Progress & logging
tqdm>=4.66.0
loguru>=0.7.0
tenacity>=8.2.2

# Tree visualization
matplotlib>=3.8.0
networkx>=3.2.0

# Utilities
python-slugify>=8.0.1

# Stage 4 — Fine-tuning
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0
accelerate>=0.27.0

# Stage 5 — Evaluation
evaluate>=0.4.0
datasets>=2.18.0

# System profiling
psutil>=5.9.0
